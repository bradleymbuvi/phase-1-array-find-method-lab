// code your solution here
function superbowlWin(record) {
    
    if (record.result === "W") {
     return (record.year)
    } 
 }

 const finalResult = record.find(superbowlWin).year
 

 function superbowlWin(record) {
    record.result === 'W'
    }



    record.find(superbowlWin).year;